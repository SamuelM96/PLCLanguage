/* Problem 4 */

function concatenation1 (language, outputNumber){
	temp = {};
	while(#temp < outputNumber){
		a = "";
		for(word : language){
			temp.add(word + a);
			if (#temp >= outputNumber){
				break;
			}
		}
		a += "a";
	}
	return temp.sort();
}